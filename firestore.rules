rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all sites collection
    match /sites/{siteId} {
      allow read: if true;
      allow write: if false;
    }
  }
}

